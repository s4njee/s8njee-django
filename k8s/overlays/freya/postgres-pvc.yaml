apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: s8njee-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
