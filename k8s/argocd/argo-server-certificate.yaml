apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-s8njee-com
  namespace: argocd
spec:
  secretName: argo-s8njee-com-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - argo.s8njee.com
