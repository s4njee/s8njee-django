# Archive

`photos-standalone-app/` contains the retired standalone Django photos service that was replaced by the single-site `blog/` application.

Treat it as historical reference only. Active development should happen in `blog/`.
